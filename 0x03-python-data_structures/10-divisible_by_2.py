#!/usr/bin/python3

def divisible_by_2(my_list):
    div_list = my_list[:]

    idx = 0
    for x in new_list:
        if x % 2 == 0:
            div_list[index] = True
        else:
            div_list[index] = False
        idx += 1

    return (div_list)
